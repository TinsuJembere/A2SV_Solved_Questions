class Solution:
    def restoreString(self, s: str, indices: List[int]) -> str:
        s_new = [""] * len(s)
        for i, c in enumerate(s):
            s_new[indices[i]] = s[i]

        return "".join(s_new)

        
