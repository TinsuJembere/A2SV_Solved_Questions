class Solution:
    def maxCoins(self, piles: List[int]) -> int:
        piles.sort()
        left, right = 0, len(piles) - 1
        n = len(piles) // 3
        res = 0

        for i in range(n, len(piles), 2):
            res += piles[i]
        return res
